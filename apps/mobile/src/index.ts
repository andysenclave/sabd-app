// Placeholder — the Expo app (Expo Router, Reanimated, SQLite, etc.) is scaffolded in T8.
// This shell exists so `pnpm -r typecheck` resolves the mobile workspace in Lane 1.
export const APP_NAME = 'Sabd';
